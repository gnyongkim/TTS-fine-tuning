# MeloTTS 자체 deps는 MeloTTS/setup.py가 설치함.
# 여기는 추가로 필요한 패키지들.

# 한국어 G2P
g2pkk>=0.1.2
jamo>=0.4.1

# MeCab (한국어 아닌 일본어용이지만 MeloTTS text 모듈이 import)
mecab-python3>=1.0.6
unidic-lite>=1.0.8

# 오디오 전처리
librosa>=0.10.1
soundfile>=0.12.1
pyloudnorm>=0.1.1

# HuggingFace Hub (사전학습 체크포인트 다운로드)
huggingface_hub>=0.20.0

# 유틸
tqdm>=4.66.0
numpy<2.0  # torch 2.1.2 호환
